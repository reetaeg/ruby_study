<p id="notice"><%= notice %></p>

<h1>Listing Product Prices</h1>

<table>
  <thead>
    <tr>
      <th>Product</th>
      <th>Price type</th>
      <th>Price</th>
      <th>Is used</th>
      <th>Target</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @product_prices.each do |product_price| %>
      <tr>
        <td><%= product_price.product %></td>
        <td><%= product_price.price_type %></td>
        <td><%= product_price.price %></td>
        <td><%= product_price.is_used %></td>
        <td><%= product_price.target %></td>
        <td><%= link_to 'Show', product_price %></td>
        <td><%= link_to 'Edit', edit_product_price_path(product_price) %></td>
        <td><%= link_to 'Destroy', product_price, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product price', new_product_price_path %>
