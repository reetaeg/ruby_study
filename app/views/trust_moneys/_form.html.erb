<!-- page content -->
<div class="right_col" role="main">
	<div class="">
	    <div class="page-title">
	      <div class="title_left">
	        <h3>Plain Page</h3>
	      </div>
	
	      <div class="title_right">
	        <div class="col-md-5 col-sm-5 col-xs-12 form-group pull-right top_search">
	          <div class="input-group">
	            <input type="text" class="form-control" placeholder="Search for...">
	               <span class="input-group-btn">
	                       <button class="btn btn-default" type="button">Go!</button>
	               </span>
	          </div>
	        </div>
	      </div>
	    </div>	
	    <div class="clearfix"></div>				
		<%= form_for(@trust_money) do |f| %>
		  <% if @trust_money.errors.any? %>
		    <div id="error_explanation">
		      <h2><%= pluralize(@trust_money.errors.count, "error") %> prohibited this trust_money from being saved:</h2>
		
		      <ul>
		      <% @trust_money.errors.full_messages.each do |msg| %>
		        <li><%= msg %></li>
		      <% end %>
		      </ul>
		    </div>
		  <% end %>
			<div class="row">
				  <div class="field">
				    <%= f.label :source_type %><br>
		    <%= f.text_field :source_type %>
				  </div>
				  <div class="field">
				    <%= f.label :agency_id %><br>
		    <%= f.collection_select :agency_id, Agency.all, :id, :name, prompt: true  %>
				  </div>
				  <div class="field">
				    <%= f.label :etc %><br>
		    <%= f.text_area :etc %>
				  </div>
					</div>
			
		  <div class="actions">
		    <%= f.submit %>
		  </div>
		<% end %>
	</div>
</div>